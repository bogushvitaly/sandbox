/**
 * Generates all permutations of an array's elements (contains duplicates).
 * This function's execution time increases exponentially with each array element.
 */

export const permutations = () => {};
