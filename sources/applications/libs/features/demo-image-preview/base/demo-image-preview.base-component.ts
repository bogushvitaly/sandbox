import { BaseComponent } from '@application/core';

export abstract class DemoImagePreviewBaseComponent extends BaseComponent {
  
  public text: string = 'DemoImagePreview';
  
  constructor() {
    super();
  }
}