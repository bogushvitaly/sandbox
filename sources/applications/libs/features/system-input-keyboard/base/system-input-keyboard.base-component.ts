import { BaseComponent } from '@application/core';

export abstract class SystemInputKeyboardBaseComponent extends BaseComponent {
  public text = 'SystemInputKeyboard';

  constructor() {
    super();
  }
}
